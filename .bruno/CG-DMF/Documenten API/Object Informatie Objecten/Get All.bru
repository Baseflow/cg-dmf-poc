meta {
  name: Get All
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/documenten/api/v1/objectinformatieobjecten
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

assert {
  res.status: eq 200
  res.body[res.body.length-1].informatieobject: endsWith {{createdId}}
}
