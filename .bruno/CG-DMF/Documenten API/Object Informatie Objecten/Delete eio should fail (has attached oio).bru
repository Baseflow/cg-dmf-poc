meta {
  name: Delete eio should fail (has attached oio)
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/documenten/api/v1/enkelvoudiginformatieobjecten/{{createdId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 400
}

settings {
  encodeUrl: true
  timeout: 0
}
