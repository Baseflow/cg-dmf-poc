meta {
  name: Get audittrail
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/documenten/api/v1/enkelvoudiginformatieobjecten/{{createdId}}/audittrail/{{firstAudit}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

assert {
  res.status: eq 200
}
