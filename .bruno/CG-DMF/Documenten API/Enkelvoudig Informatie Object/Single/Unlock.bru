meta {
  name: Unlock
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/documenten/api/v1/enkelvoudiginformatieobjecten/{{createdId}}/unlock
  body: json
  auth: inherit
}

body:json {
  {
    "lock": "{{lockId}}"
  }
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
