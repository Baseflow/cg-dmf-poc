meta {
  name: Lock
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/documenten/api/v1/enkelvoudiginformatieobjecten/{{createdId}}/lock
  body: none
  auth: inherit
}

vars:post-response {
  lockId: res.body.lock
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
