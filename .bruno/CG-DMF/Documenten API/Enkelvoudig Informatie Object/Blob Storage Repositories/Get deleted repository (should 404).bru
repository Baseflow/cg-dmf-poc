meta {
  name: Get deleted repository (should 404)
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/settings/storage-repositories/{{blobRepoId}}
  body: none
  auth: inherit
}

tests {
  test("status is 404", function() {
    expect(res.status).to.equal(404);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
