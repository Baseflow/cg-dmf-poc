meta {
  name: Get repository by id
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/admin/storage-repositories/{{repoId}}
  body: none
  auth: inherit
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
