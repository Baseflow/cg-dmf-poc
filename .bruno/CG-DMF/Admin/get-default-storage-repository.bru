meta {
  name: Get default repository
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/storage-repositories/default
  body: none
  auth: inherit
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}

