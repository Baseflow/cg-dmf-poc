meta {
  name: Set default repository
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/admin/blob-storage-repositories/default
  body: json
  auth: inherit
}

body:json {
  {
    //"name": "azure-dev"
    "name": "minio-local"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
